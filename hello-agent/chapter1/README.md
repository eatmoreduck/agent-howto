# Chapter 1: 初识智能体

这一章先理解 Agent 的基本循环：

```text
Perception -> Thought -> Action -> Observation
```

当前 demo：

- `first_agent/`：对应 Hello-Agents 1.3.1-1.3.4，做一个最小智能旅行助手。

运行：

```bash
uv run python hello-agent/chapter1/first_agent/run.py --mode mock
```
